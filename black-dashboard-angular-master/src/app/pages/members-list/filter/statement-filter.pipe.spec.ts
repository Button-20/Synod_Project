import { StatementFilterPipe } from './statement-filter.pipe';

describe('StatementFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StatementFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
